[
  {
    "tool": "nmap",
    "category": "recon/scanning",
    "command": "nmap -sS -T4 -p- 10.0.0.0/24",
    "description": "full tcp syn scan across all 65535 ports on a subnet. good first pass for host/port discovery.",
    "example": "nmap -sS -T4 -p- 10.0.0.0/24 -oN full-scan.txt",
    "tags": "nmap, scanning, recon, tcp"
  },
  {
    "tool": "nmap",
    "category": "recon/scanning",
    "command": "nmap -sV -sC -p 22,80,443 target.com",
    "description": "service/version detection plus default nse scripts on common ports. good for quick triage.",
    "example": "nmap -sV -sC -p 22,80,443 target.com -oA triage",
    "tags": "nmap, scanning, recon, service-detection"
  },
  {
    "tool": "nmap",
    "category": "recon/scanning",
    "command": "nmap -sU --top-ports 20 target.com",
    "description": "scan the top 20 most common udp ports. udp scans are slow, so start narrow.",
    "example": null,
    "tags": "nmap, udp, scanning"
  },
  {
    "tool": "wireshark",
    "category": "traffic analysis",
    "command": "tcp.port == 443 && tls.handshake.type == 1",
    "description": "display filter for tls client hello packets on port 443, useful for spotting sni/domain fronting.",
    "example": "ip.addr == 192.168.1.10 && tcp.port == 443 && tls.handshake.type == 1",
    "tags": "wireshark, tls, filter"
  },
  {
    "tool": "wireshark",
    "category": "traffic analysis",
    "command": "http.request.method == \"POST\" && http contains \"password\"",
    "description": "display filter to catch plaintext credentials being posted over unencrypted http.",
    "example": null,
    "tags": "wireshark, http, credentials, filter"
  },
  {
    "tool": "tcpdump",
    "category": "traffic analysis",
    "command": "tcpdump -i eth0 -w capture.pcap port 53",
    "description": "capture dns traffic on eth0 to a pcap file for later analysis in wireshark.",
    "example": "tcpdump -i eth0 -w dns-capture.pcap port 53 and host 8.8.8.8",
    "tags": "tcpdump, dns, capture"
  },
  {
    "tool": "tcpdump",
    "category": "traffic analysis",
    "command": "tcpdump -i any -nn -A 'tcp port 80'",
    "description": "live ascii view of unencrypted http traffic on any interface, no dns resolution.",
    "example": null,
    "tags": "tcpdump, http, live-capture"
  },
  {
    "tool": "sha256sum",
    "category": "hashing/forensics",
    "command": "sha256sum evidence.img > evidence.sha256",
    "description": "generate a sha-256 hash of a disk image to establish chain-of-custody integrity before analysis.",
    "example": "sha256sum -c evidence.sha256",
    "tags": "hashing, forensics, integrity, chain-of-custody"
  }
]
